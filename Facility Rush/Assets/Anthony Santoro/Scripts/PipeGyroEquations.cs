using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PipeGyroEquations : MonoBehaviour
{
    public bool gradeK, grade1, grade2, grade3, grade4, grade5;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
